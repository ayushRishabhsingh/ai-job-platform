# ---------------------------------------------------------------
# Core API
# ---------------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-multipart>=0.0.9        # required for file uploads

# ---------------------------------------------------------------
# Database
# ---------------------------------------------------------------
SQLAlchemy>=2.0.30
psycopg2-binary>=2.9.9         # PostgreSQL driver (not needed for SQLite)
alembic>=1.13.0                # migrations, for when create_all is not enough

# ---------------------------------------------------------------
# Auth
# ---------------------------------------------------------------
PyJWT>=2.9.0
bcrypt>=4.1.0
email-validator>=2.2.0         # backs pydantic's EmailStr

# ---------------------------------------------------------------
# Resume parsing
# ---------------------------------------------------------------
pymupdf>=1.24.0                # PDF -> text
python-docx>=1.1.0             # DOCX -> text (optional)

# ---------------------------------------------------------------
# ML / matching
# ---------------------------------------------------------------
scikit-learn>=1.5.0            # TF-IDF strategy
numpy>=1.26.0

# Embedding strategy (v3). Large download (~500 MB with torch), so it is
# commented out by default — the API degrades gracefully without it and
# returns a clear 503 if you request ?strategy=embedding.
# sentence-transformers>=3.0.0

# ---------------------------------------------------------------
# Frontend
# ---------------------------------------------------------------
streamlit>=1.37.0
requests>=2.32.0
pandas>=2.2.0

# ---------------------------------------------------------------
# Dev / test
# ---------------------------------------------------------------
pytest>=8.3.0
httpx>=0.27.0                  # required by fastapi.testclient
ruff>=0.6.0                    # linting and formatting
